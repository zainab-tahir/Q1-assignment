const arr = [1, 2, 3];

console.log(arr[3]); // Produces an index error because there is no element at index 3

// To fix the error, we can access a valid index in the array instead
console.log(arr[2]); // Outputs 3, the element at index 2
